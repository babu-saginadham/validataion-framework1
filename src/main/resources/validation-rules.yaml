modules:
  - name: module1
    attributes:
      attr1:
        min: 1
        max: 10
        message: "Attribute must be between 1 and 10"
      attr2:
        regex: "^[a-zA-Z0-9]+$"
        message: "Attribute must contain only alphanumeric characters"
  - name: module2
    attributes:
      attr1:
        min: 5
        max: 20
        message: "Attribute must be between 5 and 20"
      attr2:
        regex: "^\\d+$"
        message: "Attribute must contain only digits"
